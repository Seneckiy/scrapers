
beautifulsoup4==4.6.0
pymongo==3.4.0
selenium==3.4.3
Django==1.10.5
djangorestframework==3.6.3
mongoengine==0.13.0
django-rest-framework-mongoengine==3.3.1
lxml==3.8.0
boto3==1.0.0

# sudo apt-get install chromium-chromedriver
# chmod 777 /usr/local/bin/chromedriver

# git clone https://github.com/angular/quickstart.git test
# cd test
# npm install
# ng init --source-dir app (Make sure to accept overwriting files and tweak after if need be. This will also install angular-cli as your dev dependencies)
# npm start


# angular create new component: ng g c component_name